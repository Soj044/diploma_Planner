[tool.poetry]
name = "workestrator-contracts"
version = "0.1.0"
description = "Shared DTO contracts for Workestrator services"
authors = ["Soj044"]
readme = "README.md"
packages = [{ include = "contracts" }]

[tool.poetry.dependencies]
python = "^3.12"
pydantic = ">=2.8,<3.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
